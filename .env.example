ANTHROPIC_API_KEY=
LANGSMITH_TRACING=
LANGSMITH_ENDPOINT=
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=
DATABASE_URL=
LANGGRAPH_PG_DSN=

# Optional: private RPC overrides (defaults to public publicnode endpoints if unset)
ETH_RPC_URL=
BNB_RPC_URL=
ARB_RPC_URL=
BASE_RPC_URL=
OPT_RPC_URL=
# ENS resolution is pinned to mainnet (falls back to ETH_RPC_URL / the default if unset)
ENS_RPC_URL=

# API: comma-separated CORS origins (default *) and widget API keys
CORS_ORIGINS=
WIDGET_API_KEYS=
# SIWE auth: JWT signing secret (>=32 bytes recommended)
AUTH_JWT_SECRET=
